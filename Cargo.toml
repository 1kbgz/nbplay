[package]
name = "nbplay_py"
version = "0.1.1"
edition = "2021"
publish = false

[lib]
name = "nbplay"
path = "rust/python/lib.rs"
crate-type = ["cdylib"]

[dependencies]
nbplay = { path = "./rust", version = "*" }
pyo3 = { version = "0.29.2", features = ["abi3", "extension-module", "multiple-pymethods"] }

[profile.release]
panic = 'abort'
lto = true

[profile.test.junit]
path = "junit.xml"

[workspace]
members = [
    "js",
    "rust",
]
